War Project 环境路径：参考模组源码在 E:\mc_mod_dev\modernwar-town；构建产物部署（同步）目标为 D:\mc\.minecraft\versions\totalwar\mods\war_project-1.0.0.jar
§
War Project 规则基线（用户明确定下，勿再询问）：node 用字符串 id 作真实主键；占领始终允许（无战时/和平状态、无城镇层级）；删除全部客户端命令，只留 /warproject 服务端命令；FTB/Architectury/Xaero 均为可选依赖；Xaero 中 node 边界实线、战区边界虚线且虚线长度随缩放，世界地图最小缩放 1x，node 名称画在 node 区块几何中心（世界地图与小地图都要）；小地图几何走 Xaero FBO（注入 MinimapElementMapRendererHandler.beforeRender 的 RETURN），屏幕层只画名称并在 FBO 不可用时兜底画几何，两路以「最近 1 秒活跃」门控避免重复绘制；FTB 大地图编辑器里右键战区与右键 node 同义（都编辑其所属 node），选择菜单只留一个取消项，且仅在 node/WARZONE 编辑模式下才拦截 FTB 原有鼠标行为；node 与战区只能成对存在：删除入口只有 node delete（连带删除绑定战区，warzone delete 已删除），阵营只能由 node setfaction 下发（warzone setfaction 已删除），两者不得各自独立变更。世界地图线条为 0.5 逻辑像素亚像素细线（自建 POSITION_COLOR 顶点，因 GuiGraphics.fill 只能画 1 整逻辑像素），顶点坐标全程浮点、不得整数量化（量化会导致直角错位与断口）。用户硬性要求：不得猜测，每一步都必须先读源代码（含 javap 反编译字节码）验证。
§
War_Project × e33chat 适配（2026-09-19 落地）：队伍/同盟→e33chat 群组全部由 team/TeamE33ChatBridge 负责（纯反射调 com.niuqu.chatbubble.api.E33ChatGroupApi#replaceOwnerGroups，owner="war_project"，group id 形如 war_project:team:<teamId> / war_project:ally:<簇锚点>），同步触发点只有 team 包内三处：TeamApi.broadcast（队伍变更命令共同出口）、TeamModule.onServerStarting、TeamModule.onPlayerLoggedIn（自愈）；同盟按「同盟簇」（盟友连通分量）建组，名字前缀「盟·」。e33chat 存在且群组引擎可用时 /warproject team msg switch 只提示、队伍频道交给 e33chat 页签，否则回退本模组的队伍频道；不用自己的离线补员（登录钩子负责）。
§
War Project 归属规则（2026-09-19 用户定调）：/warproject game start|stop|end 等游戏生命周期指令属于全局命令树（command/WarProjectCommands，唯一注册点）与全局内核 module/GamePhase + module/GameStateService，不得归属任何业务模块；resource / nodeLJYS 等模块只订阅阶段变化并在各自包内反应。阶段状态刻意不落盘（开服固定 STOPPED），资源存量落盘到 SavedData war_project_resources。
§
War Project 资源经济（2026-09-20 落地为个人资源）：SavedData war_project_resources 用 players[]={id(scoreboardName),ammo,fuel}，旧 teams 段读档时忽略并记日志；node 产出按「每个在线队员各得全额」（队伍产出 X、在线 N 人各得 X），仅在线结算、离线不累积；每人每种硬上限 999（ResourceData.MAX_AMOUNT 统一 clamp）；消耗/转移全部走个人余额。服务端门面 resource/ResourceApi：playerKnown/stock/playerStocks/set/add/spend/teamRate/snapshotFor/sendSync/pushSyncAll/transfer；transfer 规则=仅 RUNNING、同队、对方在线、余额足额、对方满 999 则整笔拒绝，成功后双方各收聊天回执并刷新 HUD。指令：resource list/player/set/add/take/transfer <player> <ammo|fuel> <amount>，game status 输出 onlinePlayers/trackedPlayers/nodesWithOutput/ammo/fuel。网络协议号 "6"：ResourceSyncPacket(running,hasTeam,ammo,fuel,ammoPerMinute,fuelPerMinute,teammates[])、TransferResourcePacket(C→S)、TransferResultPacket(S→C)。
§
War_Project 常有多 agent 并行改动同一仓库：默认在自行执行 gradle 构建（./gradlew build）或把 jar 覆盖同步到 D:\mc\.minecraft\versions\totalwar\mods先检查是否有其他agent在工作，以免与其他 agent 的构建/部署冲突，若没有时照常构建并同步。本机 Gradle **daemon 模式会卡死**（daemon 日志停在 “The daemon has started executing the build” 后毫无输出，5-10 分钟无进展、CPU 停滞，杀进程重试仍复现）；改用 `.\gradlew.bat build --no-daemon --offline` 并在后台作业里用 `*>` 重定向到日志文件，约 40 秒即可 BUILD SUCCESSFUL。
§
War Project 资源 HUD/转移面板规格（2026-09-20 最终，取代旧版）：顶部居中黑色胶囊 #000000f0、无外阴影，『图标+存量+ +xx ｜ 同上』；图标用原版 PNG textures/gui/{ammo,fuel}.png 显示 12px，由 Java 原样 base64 内联，禁止预缩放或矢量重绘；岛 CEF 画布 156×18、面板 208×168、字号 9px、文案一律中文；真弧线圆角、居中由渲染方 flex 完成；**聊天栏（ChatScreen）打开时照常显示岛**（它是转移面板的点击入口），**其它任意 GUI 隐藏岛**（只显示转移面板），SBW 炮镜同样隐藏；CEF 必须按物理分辨率渲染：browser_rect=GUI 尺寸 + device_scale_factor=Minecraft.getGuiScale()，禁止『1 GUI 像素 = 1 页面像素』（会被 MC 放大 N 倍而发糊）。
§
War Project 世界地图 node 标记规格（2026-09-19 用户定调）：node 名称与其资源收益行视为一个整体、垂直居中于 node 几何中心（无收益行时名称自身居中）；收益行在缩放 <2.0x 时完全不渲染（只留名称，初版 2.5x 后由用户改为 2.0x）；图标尺寸与各间距随缩放线性变化，以 5x 为基准（基准值 图标14px / 图标-文字 2 / 两组间 6 / 行间距 2），整体系数 clamp 0.7~2.2，文字保持默认字号。缩放判定值取 Xaero `GuiMap.scale` 字段（即界面显示的 “5.16x” 那个数，已用 applyZoomLimits 常量与截图实测交叉验证）。
§
War Project × SBW（Superb Warfare）可选兼容（2026-09-19 用户定调）：玩家处于 SBW 载具第一视角（炮镜）时隐藏资源灵动岛 HUD。判定（无编译期依赖、SBW 缺失时惰性）：Minecraft.player.getVehicle() 类名沿父类链以 "com.atsuishio.superbwarfare.entity.vehicle" 开头，且 Minecraft.options.getCameraType().isFirstPerson() 为真（等价于 SBW 的 RenderContext#isFirstPerson()，javap 证据 Options.m_92176_().m_90612_()Z）。代码 client/SuperbWarfareCompat.java，被 client/ResourceHudOverlay 调用。
§
War Project 全局 HTML 渲染内核（2026-09-20 落地，html 包，零依赖、不属于任何模块）：Css / HtmlNode / HtmlDocument（解析 + refreshStyles）/ HtmlLayout / HtmlRenderer / HtmlViewHost；支持 div span b img button input hr、内联 style 与 <style> 的 .class/#id/元素/:hover/:active，以及圆角（逐行 fill 内缩）、阴影、边框、flex(row,column)、margin/padding/gap、align-items、justify-content、opacity、transform(scale,translate)、transition；关键机制：CSS 不烘焙，切 class 后靠 HtmlDocument.refreshStyles() 重求值（HtmlViewHost 用 classRevision 检测），否则 setClass 无效；img 用 11 参数 blit（drawW,drawH,uOffset,vOffset,srcW,srcH,texW,texH）；数字输入复用 vanilla EditBox、外观由内核绘制。灵动岛 client/ResourceIslandView 与转移面板 client/ResourceTransferController 都用它渲染；HTML 取自 assets/war_project/html/*.html，失败回退内置常量。转移交互：聊天栏打开时右键灵动岛 ammo/fuel 图标 → 面板出现在鼠标位置（越界夹回）→ 选本队成员（离线置灰、显示每人 A/F 存量）→ 输入数量（上限 min(自己余额, 999-对方存量)）→ Confirm/Cancel，ESC 或点面板外关闭；面板打开期间面板区域内鼠标/滚轮/按键/字符事件 setCanceled；过渡动画 150-160ms。
§
War Project 地图边界（2026-09-20 落地，用户定调）：地图区域=单个主世界矩形，语法是 `/warproject map set <fromX> <fromZ> <toX> <toZ>`（**4 个整数区块坐标、空格分隔**，两角顺序任意；旧的 `0,0 9,9` 逗号写法无效），写入 MapData 的 `bounds` 段，`/warproject map info` 只读回显；map set 会把被缩小的主世界原版边界复位到 `WorldBorder.MAX_SIZE`（不再用世界边界当硬边界）。世界里沿边界自绘黄色条纹墙（forcefield 纹理 + 与 `LevelRenderer.renderWorldBorder` 同款渲染状态，顶点取相机相对坐标，UV 0.5/格滚动，仅主世界 `Stage.AFTER_TRANSLUCENT_BLOCKS`）。Xaero 世界地图界外整片红雾 `0x38FF3B30`（POSITION_COLOR 批内最先画），小地图也画 `0x44FF3B30`（随朝向旋转，用矩形 4 角投影 + 逐行扫描求行内区间，屏幕层绘制）。越界执法 MapBoundaryService：每 tick 只判主世界，**创造/旁观模式豁免**，`Config.outOfMapReturnSeconds` 默认 30s 超时 `ServerPlayer.kill()`；准星下方红字倒计时，服务端只在新状态变化时发包（协议号 7，`OutOfMapWarningPacket`，-1 表示清除），客户端本地倒数。
§
War Project 模块改名（2026-09-20 用户定调）：wargame 模块更名为 nodeLJYS —— 包 com.flowingsun.war_project.nodeLJYS（原 wargame/）；NodeLJYSModule（id()="nodeLJYS"）← WargameModule，NodeLJYSService ← WargameService，NodeOccupationService / CaptureProgressQueryApi 随包迁移；客户端 WargameCapture* 四类 → NodeLJYSCapture*（Client / HudOverlay / HudState / NoticeHudState）；注释「reference mod's WargameCapture*」保留参考模组原名。
§
Brigadier 命令参数**只能用 vanilla 类型**（已在 `ArgumentTypeInfos` 注册）；自定义 ArgumentType 会让登录期命令树序列化抛 `Unrecognized argument type` → `Couldn't place player in world`，玩家彻底进不去存档（2026-09-20 事故）。`StringArgumentType.word()` 不含逗号，故 `/warproject map set` 用 4 个整数。写 eula.txt/datapack 必须无 BOM。
